#ifndef UTILS_H
#define UTILS_H

long double randomDouble();

long double randomDoubleWithout1();

long double randomDoubleWithout0();

bool isInteger(const char*);
bool isDouble(const char*);
bool isBoolean(const char*);

#endif
